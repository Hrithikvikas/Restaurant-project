

<%- include('./includes/head.ejs') %>
<link rel="stylesheet" href="/css/forms.css">
<link rel="stylesheet" href="/css/product.css">
</head>

<body>
<%- include('./includes/navigation2.ejs') %>

<main>
    <%= cat1_name %>
    
    <div class="grid">
    <% cat1.forEach(function(item){ %>
        <article class="card product-item">
            <header class="card__header">
                <h1 class="product__title">
                    <%= item.name %>
                </h1>
            </header>
                <h3 class="product__price">Price:
                    <%= item.price %>
                </h3>
               
           
            <div class="card__actions">
                <form action="/manager/menu" method="post">
                    <label for="quantity">Choose quantity</label>
                    <input type="number" name="quantity" id="item_quantity" step="1" min="1" max="5" required>
                    <button class="btn" type="submit">Add to Cart</button>
                    <input type="hidden" name="recipeid" value=<%= item.recipe_id %>>
                </form>
            </div>
        </article>  
    <% }); %>
    </div>

    <%= cat2_name %>
    <div class="grid">
    <% cat2.forEach(function(item){ %>
        <article class="card product-item">
            <header class="card__header">
                <h1 class="product__title">
                    <%= item.name %>
                </h1>
            </header>
                <h3 class="product__price">Price:
                    <%= item.price %>
                </h3>

            <div class="card__actions">
                <form action="/manager/menu" method="post">
                    <label for="quantity">Choose quantity</label>
                    <input type="number" name="quantity" id="item_quantity" step="1" min="1" max="5" required>
                    <button class="btn" type="submit">Add to Cart</button>
                    <input type="hidden" name="recipeid" value=<%= item.recipe_id %>>
                </form>
            </div>
        </article>  
    <% }); %>
    </div>

    <%= cat3_name %>
    <div class="grid">
    <% cat3.forEach(function(item){ %>
        <article class="card product-item">
            <header class="card__header">
                <h1 class="product__title">
                    <%= item.name %>
                </h1>
            </header>
                <h3 class="product__price">Price:
                    <%= item.price %>
                </h3>
               
            
            <div class="card__actions">
                <form action="/manager/menu" method="post">
                    <label for="quantity">Choose quantity</label>
                    <input type="number" name="quantity" id="item_quantity" step="1" min="1" max="5" required>
                    <button class="btn" type="submit">Add to Cart</button>
                    <input type="hidden" name="recipeid" value=<%= item.recipe_id %>>
                </form>
            </div>
        </article>  
    <% }); %>
    </div>


</main>